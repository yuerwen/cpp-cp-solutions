#include <bits/stdc++.h>
using namespace std;
#define int long long

signed main(){
    int t; cin >> t; 
    while (t--){
        int n, k, q; 
        cin >> n >> k >> q; 
        vector<int> a, b; 
        for (int i = 0; i < k; i++){
            int x; cin >> x; 
            a.push_back(x); 
        }
        for (int i = 0; i < k; i++){
            int x; cin >> x; 
            b.push_back(x); 
        }

        for (int aa = 0; aa < q; aa++){
            int que; cin >> que; 
            
        }
    }
}